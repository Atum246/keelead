version: '3.8'

services:
  keelead:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - keelead-data:/app/prisma
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:./prisma/keelead.db
    restart: unless-stopped

volumes:
  keelead-data:
